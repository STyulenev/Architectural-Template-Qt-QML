import QtQuick 2.15

AbstractTheme {
    id: redTheme

    mainBackgroundColor: "#000000"
    mainThemeColor: "#FF0000"

    textColor: "#FFFFFF"

    mainButtonTextColor: "#FFFFFF"
    disabledButtonTextColor: "#3C3D41"
    disabledButtonColor: "#505155"

    // другие цвета ...
}
